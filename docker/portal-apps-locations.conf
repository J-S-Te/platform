# DO NOT EDIT: managed by scripts/portal-gateway.sh.
# 任何手动修改会在下一次 add/remove/sync 时被覆盖。
# 说明：data_analysis 为随统一前端构建的内置模块，其 /api /auth /healthz 反代
# 已在 frontend/nginx/default.conf 中配置；portal-gateway 不应再为它生成整站代理。
